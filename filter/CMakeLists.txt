cmake_minimum_required(VERSION 3.16)
project(mlm_filter)            
if(CMAKE_TARGET MATCHES Build)
  message("build")
  include("build.cmake")
elseif(CMAKE_TARGET MATCHES Documentation)
  message("documentation")
  include("documentation.cmake")
else()
  message(ALERT "specify CMAKE_TARGET")
  include("build.cmake")
endif()
